apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: motron
spec:
  source:
    path: apps/motron

  destination:
    namespace: motron

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

